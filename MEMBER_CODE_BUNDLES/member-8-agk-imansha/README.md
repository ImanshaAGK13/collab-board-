# 📌 Member 8 - AGK Imansha (Cockpit App Layout, Workspace & Server Entry/Setup)

## 📁 Assigned Backend Files:
- `server/server.js`
- `server/package.json`
- `server/.env`
- `server/config/db.js`

## 💻 Git Commands for Member 8:
```bash
# 1. Main branch එක update කරගන්න
git checkout main
git pull origin main

# 2. ඔබේ feature branch එකට මාරු වී main merge කරන්න
git checkout feature/member-8-layout
git merge main

# 3. Backend files සහ frontend වෙනස්කම් add & commit කරන්න
git add .
git commit -m "feat(layout): add Express backend entry server.js, MongoDB connection & Socket.io server"

# 4. GitHub එකට Push කරන්න
git push origin feature/member-8-layout
```
